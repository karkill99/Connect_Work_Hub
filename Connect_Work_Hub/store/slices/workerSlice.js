import { createSlice } from "@reduxjs/toolkit";

const workerSlice = createSlice({
    name: "worker",
    initialState:[],
    reducers: {
      
    }
})
// console.log(workerSlice.actions)
export {workerSlice};
export const {} = workerSlice.actions